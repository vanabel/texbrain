# pnpm 9+ requires `packages` when `pnpm-workspace.yaml` is present (even for a single root package).
packages:
  - '.'

allowBuilds:
  esbuild: true
patchedDependencies:
  '@vanabel/texlyre-busytex@0.1.6-beta': patches/@vanabel__texlyre-busytex@0.1.6-beta.patch
